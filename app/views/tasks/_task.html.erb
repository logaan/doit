<li class="task">
  <span class="name"><%= task.name %></span>
  <%= link_to('edit', edit_task_path(task), :class => 'edit_task') if logged_in? %>
  <%= link_to('delete', task_path(task), :method => 'delete', :class => 'destroy_task') if logged_in? %>
</li>
